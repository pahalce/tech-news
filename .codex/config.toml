[features]
codex_hooks = true

[[hooks.Stop]]
[[hooks.Stop.hooks]]
type = "command"
command = 'sh "$(git rev-parse --show-toplevel)/.agents/hooks/run-quality-checks.sh"'
timeout = 600
statusMessage = "Running vp check --fix and architecture check"
